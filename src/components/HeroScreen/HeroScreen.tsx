import React, { FC } from 'react';
import styles from './HeroScreen.module.css'





const HeroScreen:FC = () => {


    return (
        <section className={styles.HeroScreen} >
            <div className={styles.HeroScreen_bgBottom} ></div>
        </section>
    );
};

export default HeroScreen;